<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、<%= link_to "#{current_user.nickname}さん", "/users/#{current_user.id}", class: :greeting__link %>
      </div>
    <% end %>
    <%# // ログインしているときは上記を表示する %>
    #<%= @tag.tag_name %> 
    <div class="card__wrapper">
      <%# 投稿一覧を表示する %>
      <%= render partial: "album", collection: @albums %>
    </div>
  </div>
</main>